node_modules/
dist/
*.log
.DS_Store
.env
.env.local

# Рабочие скриншоты агентов — пересоздаются, в истории только шум
.screenshots/

# design-sync: staged converter, build output, machine state
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
# декларации для дизайн-системы — генерируются tsconfig.ds.json
web/dist-types/

# Инструмент design-sync: состояние машины, не код продукта
.design-sync/
web/tsconfig.ds.json
# Генерируемые декларации и кэш tsc
shared/api.d.ts
*.tsbuildinfo

# Скриншоты регрессионного UI-прогона (tests/ui) — пересоздаются каждым запуском
tests/ui/.out/
tests/ui/webkit/.out/

# PDF-версии инструкций — генерируемые артефакты (docs/build-pdf.mjs), в git не держим.
docs/*.pdf

# Артефакты дымового прогона установленного приложения (CI, Windows).
installed-smoke/
