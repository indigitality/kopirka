# Долг по дизайну

Замечания Сергея от 30.08.2026 по живому приложению. Разбираются **отдельной веткой
после упаковки в приложение** — его решение, порядок не менять.

Первоисточник — артборд `1SC-0` в Paper и [DESIGN-SPEC.md](./DESIGN-SPEC.md).
Общий принцип, который я нарушил: **не изобретать того, чего нет в макете.**
Каждое замечание ниже — это моя отсебятина, а не расхождение в трактовке.

## Исправлено

- [x] **Оболочка была трёх разных тонов.** Фон `#0a0a0b`, сайдбар и верхняя панель
      `#0f0f11` — разделение слоёв, которого в макете нет. Сведено к единому чёрному
      `#000000`; приподняты только поле поиска, поповеры и модалки.
      Заодно убрана разделительная линия у подвала сайдбара — её тоже не было в макете.
- [x] **Бейдж `⌘K` в поле поиска не по макету.** *02.09.2026.* Плашка была
      `--color-surface-active` на `--color-surface-raised` — 1.06:1, её просто не было
      видно. Стало: `--color-surface-chip` (#1c1c22), текст `--color-ink-faint` (4.34:1),
      радиус `--radius-xs`, паддинг 2/5, правый отступ 6 px. Замерено живьём.
- [x] **Активная строка выглядит тяжелее, чем в макете.** *02.09.2026, решение D9.*
      Дело было не в тяжести, а в том, что ховер и выбор красились одной заливкой:
      наведение выглядело как выбор. Роли разведены — ховер
      `--color-surface-row-hover` (белый 8%), активная `--color-surface-row` (14%)
      плюс `--color-ink` и вес 500.

- [x] **Переключателя размера сетки не было, сортировка была спрятана в «Фильтр».**
      *02.09.2026, решения D5 и D6.* В верхней панели появился правый ряд
      `[ Сначала новые ▾ ] [ ▦ ▦▦ ▦▦▦ ] [ Фильтр ]`. Три целевые ширины колонки
      (560 / 275 / 180) с клемпом минимума `{l:2, m:3, s:4}` дают на 1440 ровно
      2 / 4 / 6 колонок — замерено живьём. Хоткеи ⌘1 / ⌘2 / ⌘3.
- [x] **Свёрнутый сайдбар нельзя было вернуть мышью.** *02.09.2026.* Слева в верхней
      панели кнопка `PanelLeft` 28×28 с тултипом «Сайдбар ⌘\».
- [x] **Не было заголовка контента: папка «Сэбач» выглядела как вся библиотека.**
      *02.09.2026, решение D8.* Строка 44 px над сеткой с именем и счётчиком;
      шапка корзины со «склонением 1 файлов» переехала туда же и получила `plural`.
- [x] **Плавающие панели центрировались по окну и налезали друг на друга.**
      *02.09.2026.* Одна «полка» `#kopirka-shelf` в области контента: панель
      выделения → прогресс импорта → тосты, порядок через `order`.
- [x] **Вид не переживал перезапуск.** *02.09.2026.* Ключ `kopirka.view`
      с `gridSize`, `sort`, `sidebarCollapsed`, `collapsedFolderIds`.
      Фильтры и поиск нарочно не сохраняются.

## Открыто

- [ ] **Нет зазора между иконкой папки и названием.** В строке дерева иконка и текст
      слиплись (`📁Пух перо`). Слот иконки 22px, сама иконка 14px — зазор съеден
      выравниванием. Чинить, не ломая вертикальные полосы: иконка, имя и трейлинг-кнопка
      должны остаться на одной вертикали во всех строках независимо от вложенности.
      *Частично закрыто 02.09.2026: разделы получили иконки в тот же слот, иконки всех
      строк стоят на x = 28, текст — на x = 52 (замерено). Сам зазор 8 px сверить с `2-0`.*
- [ ] **Паддинги строк сайдбара не по макету.** Проверить по `1SC-0`: горизонтальные
      отступы строки, отступ на уровень вложенности, высота строки, вертикальные
      промежутки между разделами и деревом.

- [ ] **Карточка не подстраивается под маленький размер.** При ширине колонки < 220 px
      (на 1440 это «маленький», на 900 — уже «средний») бейдж имени папки съедает
      четверть кадра, чекбокс 20 px великоват, радиус `--radius-md` читается как мыло.
      Нужен проп `columnWidth` у `GridCard` — файл за агентом карточки (дизайн-аудит §3.5).
- [ ] **Оси фильтра «Ориентация», «Источник», «Домен»** ждут параметров в `FileListQuery`
      и эндпоинта `GET /api/domains` (дизайн-аудит §3.2).

## Как сверять

Лимит Paper MCP сбрасывается примерно 4 сентября 2026. После этого можно снять точные
значения через `get_computed_styles` и `get_jsx` по артборду `1SC-0` и сверить с
`tokens.css` — вместо того чтобы снимать на глаз со скриншотов, как делалось сейчас.
