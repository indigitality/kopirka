/**
 * Внешние адреса интерфейса.
 *
 * Открываются они не программно, а обычной ссылкой с `target="_blank"`: в окне
 * приложения запрос на новое окно перехватывает десктопный слой
 * (`desktop/src-tauri/src/windows.rs`, `on_new_window` → `open_externally`) и
 * отдаёт адрес системному браузеру. Тот же путь у «Открыть источник» в панели
 * деталей — отдельной функции нет и заводить её незачем.
 */

/**
 * «Сообщить об ошибке» в подвале сайдбара (R13 · логотип и подвал).
 *
 * Личный телеграм Сергея — адрес взят из вики клуба (`TECH_CONTEXT`). Это не
 * канал `t.me/orshks`: туда писать некуда. Когда у клуба появится отдельный чат
 * поддержки, Сергей заменит адрес здесь — больше нигде он не встречается.
 */
export const BUG_REPORT_URL = 'https://t.me/orshaks';

/**
 * Пропы внешней ссылки. `noopener` обязателен: без него открытая вкладка
 * получает `window.opener` на интерфейс приложения.
 */
export const EXTERNAL_LINK_PROPS = {
  target: '_blank',
  rel: 'noreferrer noopener',
} as const;
